<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .main__container {
            width: 100vw;
            max-width: 600px;
            margin: 100px auto 0;
         
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 2rem;
            font-weight: bold;
        }

        .form-label {
            font-weight: bold;
            
        }

        .form-control {
            padding: 10px;
            font-size: 1rem;
        }
    </style>
</head>
<body>

    <div class="main__container shadow-lg p-4 bg-light rounded">
        <h2>Login Form</h2>

        <!-- Display any errors -->
        {{#errors}}
        <div class="alert alert-danger" role="alert">
            <ul>
                {{#errors}}
                    <li>{{.}}</li>
                {{/errors}}
            </ul>
        </div>
        {{/errors}}

        <!-- Display remaining login attempts only if there are attempts left -->
        {{#remaining_attempts}}

        {{/remaining_attempts}}

        <!-- Form fields, disable inputs if form_disabled is true -->
        <form action="/login" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label">Username:</label>
                <input type="text" class="form-control" id="username" name="username" placeholder="Username or Email" required {{#form_disabled}}disabled{{/form_disabled}}>
            </div>

            <div class="mb-3">
                <label for="password" class="form-label">Password:</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Password" required {{#form_disabled}}disabled{{/form_disabled}}>
            </div>

            <!-- Hide the login button if form is disabled -->
            <div class="d-grid gap-2">
                {{^form_disabled}}
                <button type="submit" class="btn btn-primary">Login</button>
                {{/form_disabled}}
            </div>
        </form> 

        <!-- Display message if the form is disabled -->
        {{#form_disabled}}
        <p class="alert alert-warning mt-3">Too many failed attempts. Please try again later.</p>
        {{/form_disabled}}

    </div>

</body>
</html>
